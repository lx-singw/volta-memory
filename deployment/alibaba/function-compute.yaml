# [DEPRECATED]
# This ROS template has been deprecated in favor of Serverless Devs (s.yaml)
# at the repository root, which supports modern Function Compute 3.0 deployments.
#
# Refer to root s.yaml for current serverless backend deployment.

